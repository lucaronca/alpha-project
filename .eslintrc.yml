parser: babel-eslint
extends:
  - airbnb-base

rules:
  arrow-parens: off
  global-require: off
  import/prefer-default-export: off
  import/no-named-default: off
  max-len:
    - error
    - code: 120
      ignoreComments: true
  semi:
    - error
    - never

env:
  node: true
  jest: true

settings:
  import/resolver:
    node:
      moduleDirectory:
        - node_modules
        - src
